# v4.08 동작 정확도 학습 구조

## 핵심 변경

- MediaPipe는 사람의 33개 관절좌표를 찾는 검출기로만 사용합니다.
- 별도 동작 모델은 33개 관절의 전체 시간축 궤적, 손발의 동시성, 중심 이동, 완성 자세를 학습합니다.
- 관절각 하나가 비슷하다는 이유만으로 올바른 동작으로 판정하지 않습니다.
- 전문가가 승인하지 않은 영상은 `pending_expert_review`로 저장되며 학습에서 자동 제외됩니다.
- 동작별 승인 표본이 5개 미만이면 모델을 만들지 않고 기존 보수적 평가만 사용합니다. 20개 이상부터 신뢰도가 1.0에 도달합니다.

## 파일1 태백 재검증

- 파일: `KakaoTalk_20260723_101323061.mp4`
- 올바른 품새 설정: 태백 26동작
- 앱 검출: 준비 0.0~5.0초, 첫 동작 `왼범서기 아래손날헤쳐막기` 5.0초 시작, 본동작 종료 48.1초
- 경계와 검수 상태는 `data/ground-truth/taebaek-file1-review.json`에 기록했습니다.
- 업데이트된 앱에서 분석 JSON을 저장하면 `learning_dataset_candidate`에 정규화된 33개 관절 시퀀스가 포함됩니다.

## 전문가 검수 항목

2026 경기규칙의 태백 세부 기준대로 각 구간에 `label.action_correct=true/false`와 오류코드를 기록합니다. 예: 아래손날헤쳐막기의 범서기 여부, 손 위치가 뒷발 허벅지 앞선인지, 손등 방향, 손날이 허벅지 선을 벗어나는지 등입니다.

검수가 끝난 데이터는 다음처럼 표시합니다.

```json
{
  "review": {
    "status": "expert_approved",
    "action_labels_reviewed": true,
    "boundary_reviewed": true
  }
}
```

## 무료 로컬 학습

```powershell
node scripts/train-action-model.js approved-1.json approved-2.json approved-3.json approved-4.json approved-5.json
```

결과는 `www/models/action-quality-v1.json`에 저장되며 다음 앱 분석부터 자동 로드됩니다. 인터넷이나 유료 API가 필요하지 않습니다.
