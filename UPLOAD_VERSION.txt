태권도 품새 수련 v3.68 스냅샷 캡처 속도 개선 + 실영상 테스트 계속

2026-07-18 v3.68 구간 스냅샷 캡처에 빠른 seek 적용
- 배경: v3.67 크래시 수정 후, 같은 실제 영상(태극6장)으로 재검증 계속 진행. 크래시는
  재현되지 않고 "파워·맺음 평가 완료(100%)"까지 두 번 연속 깨끗하게 도달하는 것을
  확인(크래시 수정 확실히 유효함).
- 스냅샷 캡처가 정밀 페인트 대기(waitForSeek 기본값)를 쓰고 있었는데, 스냅샷은
  화면 참고용이라 프레임 하나 오차가 문제되지 않음 — 필름스트립 생성 때 이미
  쓰던 빠른 seek(fast=true)로 통일. 메인 분석 경로와 수동 재조정 경로 둘 다 적용.
  같은 실제 영상으로 재측정한 결과 평가 단계가 약 15% 빨라짐(188초→159초,
  GPU 가속 없는 이 테스트 환경 기준).
- **한계, 솔직히 말씀드립니다**: 이 개발 환경은 GPU 가속이 전혀 없는(소프트웨어
  렌더링) 헤드리스 브라우저라, 실제 사용자 브라우저보다 훨씬 느릴 가능성이 커요.
  평가 완료(100%) 이후 최종 리포트 객체가 만들어지기까지 이 환경에서 100초
  넘게 걸려서, 그 마지막 단계까지는 관찰하지 못했습니다. 코드상으로는 평가 완료
  직후 거의 동기적으로 리포트가 만들어지는 구조라(추가 대기 없음), 실제
  사용자 기기(GPU 가속 있음)에서는 이런 지연이 없을 가능성이 높지만, 이건
  제가 직접 확인 못 한 부분이라 실사용 테스트가 필요합니다.
- 태극6장 19개 동작명 데이터는 이미 전부 채워져 있음을 재확인(플레이스홀더 없음).
- 회귀 테스트는 기존 그대로 통과(새 API 추가 없이 기존 waitForSeek(time, true)
  패턴을 한 곳 더 쓴 것뿐이라 새 검증 항목 없음).
- 서비스워커 캐시 버전: www v17→v18.

태권도 품새 수련 v3.67 [긴급] 분석 완료 직전 크래시 버그 수정

2026-07-18 v3.67 실제 태권도 영상으로 처음 실사용 테스트 — 크래시 버그 발견 및 수정
- 배경: 사용자가 실제 태권도 영상(태극6장, 19동작)과 국기원 품새선 이미지를 제공해
  처음으로 진짜 영상으로 전체 파이프라인을 검증할 기회가 생김. 이 환경은 헤드리스
  Chromium이라 HEVC(H.265)뿐 아니라 일반 H.264도 재생이 안 되는 제약이 있어서,
  시스템 ffmpeg(/usr/bin/ffmpeg, Playwright 번들용 ffmpeg와는 별개)로 WebM으로
  변환해 우회, 실제 앱 UI를 자동화해 품새 선택 → 영상 업로드 → 분석 시작까지
  진짜 흐름으로 실행함.
- **발견한 크래시**: 파워·맺음 평가가 전부 끝나고(20/20, 100%) 결과를 요약하는
  마지막 단계에서 `ReferenceError: boundaryDetectionForRun is not defined`로
  매번 죽고 있었음. 원인: v3.66에서 박수 감지 안내 문구를 추가하면서
  `summarize()` 함수 안에서 다른 함수 스코프에 있는 변수를 잘못 참조함(테스트는
  가짜 상태를 직접 주입해서 실제 summarize() 호출 경로를 안 거쳐 못 잡았음 —
  실제 영상 없이는 발견 불가능했던 버그).
- 수정: `summarize(results, view)` → `summarize(results, view, clapInfo)`로
  박수 정보를 명시적 매개변수로 전달하도록 변경. 스코프 문제 근본 해결.
- 검증: 수정 전 영상으로 재현 → 크래시 확인 → 수정 → 같은 영상으로 재실행 →
  파워·맺음 평가 완료(100%)까지 에러 없이 도달 확인. 이 환경(GPU 가속 없는
  헤드리스 브라우저)에서는 구간별 캡처 사진 촬영 단계가 느려서(초당 수 프레임
  순차 탐색) 최종 리포트 객체 생성까지는 못 봤지만, 크래시 없이 계속 진행되는
  것은 확인함 — 실제 사용자 브라우저(GPU 가속 있음)에서는 훨씬 빠를 것으로
  예상됨.
- 태극6장 데이터는 이미 19개 동작명이 전부 채워져 있음을 확인(placeholder 없음).
- 회귀 테스트(verify-cross-check) 3건 추가(summarize 시그니처·호출부 고정).
- 서비스워커 캐시 버전: www v16→v17.

태권도 품새 수련 v3.66 박수 기반 분석 시작점 + 스켈레톤 오버레이

2026-07-18 v3.66 박수 시점 감지 + 재생 영상 관절선(스켈레톤) 표시
- 배경: "정리본" 3번 항목(박수 인식 분석 시작, 스켈레톤 오버레이)을 이전 세션에서
  이미 반영했다고 잘못 보고했음(단어 검색만 하고 실제 내용을 확인 안 한 실수 —
  "박수"는 다중 카메라 동기화 안내 문구였고, "스켈레톤"은 분석 방식 설명 문장
  한 줄이었을 뿐, 실제 기능이 전혀 없었음). 사용자가 재차 지적해서 발견, 이번에
  실제로 만듦.
- 박수 시점 감지: 손목 좌표(이미 계산되는 데이터 재사용, 오디오 분석 아님)가
  급격히 가까워졌다 벌어지는 패턴을 영상 초반에서 탐색. 뚜렷한 신호가 있으면
  그 지점+0.15초를 분석 시작점으로 우선 사용(기존 정점 기반 시작점 탐지보다
  신뢰). 못 찾으면 조용히 기존 방식으로 대체 + 안내 문구로 "박수를 다시 크게
  쳐보라"는 팁 표시. 분석 요약에 감지 여부와 시각 표시.
- 스켈레톤 오버레이: video 태그 위에 캔버스를 얹어 관절선을 그림. 데이터는 새로
  검출하지 않고 분석 중 이미 수집된 lastBoundaryCache.frames(영상 전체 초당
  약 2.6샘플)를 재생 시각 기준으로 선형 보간(interpolate)해서 부드럽게 이음
  (간격 1.2초 넘으면 보간 대신 더 가까운 프레임 사용 — 사람이 화면 밖으로
  나갔다 온 경우 등 대비). 신뢰도(visibility)에 따라 초록/노랑/빨강(점선) 색상
  구분. object-fit:contain 레터박스를 감안해 실제 화면 표시 영역에 정확히
  맞춰 그림. ON/OFF 스위치는 localStorage에 저장(기존 테마 설정과 동일한
  방식 재사용). 메인 재생 화면과 구간 반복 재생 팝업 둘 다 적용 — 같은
  requestAnimationFrame 루프가 두 영상을 함께 갱신해서 반복 재생에도 끊김 없음.
- 채점/분석 로직(velocityScore 등)과 경계탐지 알고리즘 본체는 건드리지 않음
  — 박수 감지는 시작점 하나만 조정하는 선택적 오버라이드, 스켈레톤은 순수
  표시용이라 분석 결과에 영향 없음.
- **실수 하나 발견 및 즉시 수정**: 스켈레톤 코드 삽입 중 str_replace 실수로
  captureVideoSnapshot 함수 선언부(function/try 줄)가 통째로 지워지는 문법
  오류를 만들었음. node --check로 정확한 위치를 찾아 그 자리에서 바로 수정 —
  배포 전 자동 테스트(module script 문법 검사)에서 잡힘, 실제 배포되지 않음.
- 합성 테스트로 검증: 손목 접근 패턴 기반 박수 감지 3케이스, 좌표 보간(중간점/
  분수점/범위 밖/큰 간격 처리) 8케이스 — 총 14개 단위 테스트 통과. 실제 브라우저
  Playwright로 토글 상태 저장/복원, 캔버스에 실제 픽셀이 그려지는지(활성화 시
  약 2300픽셀 감지, 비활성화 시 0픽셀로 완전히 지워짐)까지 확인.
- 실제 태권도 영상으로는 검증 못함(이 환경엔 없음) — 특히 박수 감지 민감도와
  스켈레톤이 실제 체감상 매끄러운지는 실사용 확인이 필요함.
- 회귀 테스트(verify-cross-check) 10건 추가.
- 서비스워커 캐시 버전: www v15→v16.

태권도 품새 수련 v3.65 파트너 배너 + 앱 내 관리자 모드 추가

2026-07-18 v3.65 하단 흐름 배너(기간제) + PIN 기반 관리자 패널
- 배경: "정리본" 문서로 요청받은 4개 항목 중 3번(스켈레톤 오버레이)·4번(공식 동작
  카드 제거)은 이미 반영되어 있었음(3번은 v3.56~59 어딘가에서, changelog 기록이
  누락된 채로 — 이번 기회에 발견). 1번(파트너 배너)·2번(관리자 모드)은 코드가
  전혀 없어서 이번에 새로 만듦.
- 파트너 배너: `app_config/banner` Firestore 문서로 이미지 URL·링크·시작/종료
  일시(한국시간)·노출 여부를 관리. 로드 시 + 1분마다 재확인해서 종료 시각이
  지나면 앱을 켜둔 채로도 자동으로 사라짐. 흐름(marquee) 애니메이션 10초 주기,
  마우스 올리면 정지, prefers-reduced-motion 감지 시 애니메이션 없이 중앙 고정.
- 관리자 모드: 하단의 작고 눈에 안 띄는 "관리자 모드" 버튼 → PIN(0713) 확인 →
  배너 이미지/링크/기간/노출 여부 편집 + "즉시 숨기기" 버튼. 별도 웹페이지 접속
  불필요.
- **Firebase 프로젝트 공유 주의**: firestore.rules 확인 결과 이 프로젝트가
  momgagym-cms(회원/매출 관리)와 같은 Firebase 프로젝트를 씀. 배너 데이터는
  기존 corrections/CMS 컬렉션과 완전히 분리된 새 경로(`app_config/banner`)에
  저장하고, 규칙도 그 경로에만 적용되도록 분리해서 작성함.
- 보안 설계(의도적 타협, 사용자에게 안내함): 로그인 시스템이 없어 PIN 방식 사용.
  화면에서 PIN 확인 + Firestore 규칙에서도 PIN 재검증(이중 장치)이지만, 문서에
  pin 필드가 함께 저장되어 기술적으로는 조회 가능 — 회원/매출 같은 민감 정보가
  아닌 홍보 배너용이라 이 수준으로 설계. **PIN을 바꾸려면 index.html의
  ADMIN_PIN 상수와 firestore.rules의 값을 함께 바꿔야 함.**
- **실제 배포 필요**: 로컬 firestore.rules 파일만 갱신했음. Firebase 콘솔(또는
  Firebase CLI)에서 실제 규칙을 이 내용과 합쳐 배포해야 배너 저장이 실제로
  동작함 — 기존 CMS 쪽 다른 컬렉션 규칙이 있다면 덮어쓰지 말고 병합할 것.
- Firebase(gstatic.com, firestore API)는 이 개발 환경 네트워크에서 접근 불가해
  실제 저장/불러오기는 검증 못함. 대신 시간 판정 로직(한국시간 파싱·왕복 변환,
  노출 여부 4가지 경우)과 UI 동작(PIN 오답/정답, 필드 표시, marquee 애니메이션,
  reduced-motion 대응)은 가짜 데이터로 end-to-end 확인함.
- 회귀 테스트(verify-cross-check) 7건 추가.
- 서비스워커 캐시 버전: www v14→v15.

태권도 품새 수련 v3.64 서기(자세) 교차검증 추가 — 경계 밀림 감지

2026-07-18 v3.64 동작명 기대 서기 vs 감지 서기 비교로 구간 밀림 신호 추가
- 배경: 기존 경계탐지는 움직임 신호만 보고 구간을 나눈 뒤 순서대로 번호를 매길 뿐,
  그 구간 내용이 실제로 해당 번호 동작이 맞는지 확인하지 않았음("영상-분석 통일성
  원칙"은 순서 매핑만 보장하지 내용 일치는 보장하지 않음). "정답 사진 DB"는 사람마다
  자세가 달라 신뢰 기준이 되기 어렵다는 논의 끝에, 사람 개인차에 흔들리지 않는
  "서기 종류"(앞굽이/뒷굽이/주춤서기/모아서기) 판별로 방향을 잡음.
- 국기원 2026 경기규칙 기본동작(서기) 채점기준의 객관적 수치(무릎 각도·체중비)를
  근거로 classifyStanceFromLandmarks 작성: 양 무릎 중 편 다리/굽힌 다리 패턴 +
  발목 간격(몸통 길이 대비)으로 서기 종류를 분류. "자세가 예쁜가"가 아니라
  "다리가 펴져 있나 굽어 있나" 같은 이분법적 특징만 사용해 개인차에 강건하게 설계.
- 동작명에 이미 있는 서기 키워드(expectedStanceFor)와 감지된 서기를 비교
  (checkStanceMatch)해서 불일치 시 구간 카드에 "⚠ 서기 불일치" 배지 표시.
  구간 완료 시점 부근(마지막 25%) 프레임 다수결로 판정하고, 60% 미만 합의면
  판별하지 않음(애매한 건 조용히 넘어감 — 거짓 경고 방지).
- 정면 카메라는 앞/뒤 깊이 구분이 약해 앞굽이·뒷굽이 신뢰도를 낮춤(측면·대각선
  카메라가 이 검증에는 더 유리함).
- 범서기·학다리서기·꼬아서기 등 드문 서기는 오판 위험이 커서 이번 버전에서는
  판별하지 않음(null 반환, 조용히 스킵) — 기존 "동작명 임의로 안 만든다" 원칙과
  같은 이유로, 자신 없는 판정은 하지 않음.
- 채점/분석 로직(velocityScore 등 기존 5개 점수) 자체는 전혀 건드리지 않음 —
  완전히 새로운 독립적 신호로 추가.
- 국기원 자료 기준 좌표를 기하학적으로 직접 구성한 합성 테스트로 분류 로직 검증
  (18개 케이스: 4개 서기 명확한 사례, 정면/측면 신뢰도 차이, 동작명 파싱, 불일치
  감지 end-to-end). 실제 태권도 영상으로는 아직 검증 못함 — 실사용 데이터로
  임계값(현재 무릎각도·폭 기준) 튜닝이 필요할 수 있음.
- 회귀 테스트(verify-cross-check) 6건 추가.
- 서비스워커 캐시 버전: www v13→v14.

2026-07-18 v3.63 보정 헤더 버전 표시 동기화(v3.60 → v3.63)
- 화면 헤더의 버전 문구(index.html 상단 소개글)가 v3.60에 고정된 채 v3.61~v3.63
  내용 변경과 따로 놀고 있었음을 확인(사용자가 실서비스에서 발견). v3.63으로 수정.
- 회귀 테스트도 새 버전 문구를 검사하도록 갱신 — 앞으로 버전 올릴 때 헤더 표시를
  빠뜨리면 테스트에서 바로 걸리도록 함.

태권도 품새 수련 v3.63 처음 접하는 사람·초등학생 기준 UI 단순화

2026-07-17 v3.63 위저드·결과·수동편집 화면 쉬운 말/단계적 노출로 정리
- 배경: 매번 만나는 4단계 위저드와 결과 화면에 "강유(剛柔)", "구간 감지 민감도", "교차검증"
  같은 내부 용어가 그대로 노출되어 있었고, 촬영 방식은 폰 1~3대 옵션 4개가 동일한 무게로
  나열되어 있어 처음 쓰는 사람·초등학생 수련생 기준으로 첫 화면부터 판단할 게 많았다.
- 1단계: 수련품새 탭에 "처음이면 이거" 배지 추가. 강유 분석 토글을 "고급(선택)"으로 표시하고
  설명을 "언제 힘있게, 언제 부드럽게"처럼 쉬운 말로 교체(몰라도 된다는 안내 포함).
- 3단계: "정면 1대"를 "추천" 배지와 함께 기본으로 크게 보여주고, 폰 2~3대(정면+측면/3방향)
  옵션은 "더 정확하게 찍고 싶다면 (선택)" 접이식 영역으로 이동. 이미 폰 2~3대를 선택한
  상태라면 접이식 영역이 자동으로 펼쳐진 채 보여 현재 선택이 숨겨지지 않게 처리.
- 4단계: "구간 감지 민감도"(내부 경계탐지 파라미터)를 "동작이 잘못 나뉘면 여기서 조정
  (고급, 보통은 그대로 두세요)" 접이식 영역으로 이동. 기본값(매우 높음·권장)은 그대로.
- 결과 화면: "판정 범위 및 교차검증 안내"는 수련품새 모드에서 "이 결과에서 알 수 있는 것"으로,
  "핵심 모션 매칭 피드백"은 "시선·자세·발동작 확인/체크"로, "지도자 추천 피드백 및 훈련법"은
  수련품새에서 "다음에 연습할 점"으로, "전체 부분"은 "동작 하나씩 자세히 보기"로 표현을
  쉽게 바꿈. 내보내기용 텍스트 리포트(formatReportText)의 공식 항목명(1./2./3.)은
  그대로 유지 — 화면 표시만 접근성 있게 바꾸고 기록용 문서 형식은 건드리지 않음.
  경기품새 모드의 "판정 범위 및 교차검증 안내" 문구도 그대로 유지(코치 대상 문서 성격).
- 수동 조정 안내 문구를 "🎞 프레임으로 찾기를 누르면 사진을 보고 탭으로 고를 수 있어요"로
  바꿔 새로 생긴 필름스트립 기능을 바로 안내하도록 함.
- 새 접이식 영역(advanced-fold)의 탭 영역을 48px 이상으로 맞춰 기존 모바일 터치 기준 유지.
- 회귀 테스트: 문구 변경으로 깨진 기존 검증 1건("전체 부분" → "동작 하나씩 자세히 보기")을
  같은 의도로 갱신. 그 외 기존 검증은 모두 그대로 통과(다른 위치에 원문 유지됨을 확인).
- 서비스워커 캐시 버전: www v11→v12.

태권도 품새 수련 v3.62 경계·분할 지점 프레임 필름스트립 추가

2026-07-17 v3.62 구간 수동 조정에 '프레임으로 찾기' 추가
- 배경: 기존 수동 조정은 -0.1초/+0.1초 미세조정과 시각 직접 입력만 있어서, 정확한 시각을
  모른 채로는 경계를 어디로 옮겨야 할지 알기 어려웠다(영상을 따로 스크럽해서 시각을 읽고
  다시 입력창에 타이핑해야 함). "편집 수정이 어렵다"는 문제의 핵심 원인.
- 수정: 경계 조정 칸과 "둘로 나누기" 칸에 각각 "🎞 프레임으로 찾기" / "🎞 나눌 지점 찾기"
  버튼을 추가. 누르면 현재 경계(또는 구간 중앙) 주변의 프레임 9장을 실제로 캡처해 가로
  스크롤 필름스트립으로 보여주고, 원하는 프레임을 탭하면 그 정확한 시각으로 즉시 적용된다
  (기존 adjustBoundary/splitSegment를 그대로 사용, 새 채점·경계탐지 로직 없음).
- 경계 조정은 현재 위치 기준 ±0.9초(9장, 약 0.22초 간격) 범위에서 찾고, 앞뒤 구간을 침범하지
  않도록 기존 가드(±0.1초 여유)를 그대로 따른다. 분할 지점은 구간 전체 범위에서 찾는다.
  한 번 고른 뒤 다시 열면 새 위치를 기준으로 재탐색되어, 필요하면 반복해서 좁혀갈 수 있다.
- 썸네일은 정밀 스냅샷(360px)보다 가벼운 120px 저해상도로 빠르게 생성. 탭 영역은 약
  86×74px로 모바일 최소 터치 크기(48px) 기준을 넉넉히 만족.
- 실제 채점/분석 로직(evaluateSegment, 경계탐지)은 전혀 건드리지 않음 — 이미 있는
  경계 이동·분할 함수에 더 정확한 시각을 입력하는 통로만 추가한 것.
- 회귀 테스트(verify-cross-check) 5건 추가.
- 서비스워커 캐시 버전: www v10→v11.

태권도 품새 수련 v3.61 A4 카드 리포트에 동작별 캡처 사진 추가

2026-07-17 v3.61 우선 보완할 구간 카드에 캡처 프레임 삽입
- 배경: 구간별 대표 장면(완료 시점 캡처, captureVideoSnapshot)은 이미 분석마다 기록되어
  화면 결과 카드(scene-shot)에는 표시되고 있었지만, A4 카드 JPG 리포트의 `우선 보완할
  구간`에는 순위·제목·피드백 텍스트만 그려지고 사진이 빠져 있었다.
- 수정: saveJpgReport를 비동기로 전환해 캡처 이미지를 먼저 로드(loadImageAsync)한 뒤,
  우선 보완할 구간 각 줄에 순위 배지 옆으로 캡처 프레임을 비율 유지(letterbox, shot 함수)로
  그리도록 함. 캡처가 없는 구간은 화면 카드와 동일하게 "장면 없음"으로 표시.
- 카드 섹션 높이를 구간 수에 맞춰 그리기 전에 미리 계산(캔버스는 생성 후 크기를 바꾸면
  그림이 지워지므로 최종 높이를 사전 확정)해, 아래 안내 박스·하단 타임스탬프 위치도 함께
  밀려 겹침 없이 표시됨. 우선 구간이 4개 미만인 품새에서도 정상 동작.
- 리포트 내용 범위는 기존과 동일하게 유지(품새 전체 요약 + 우선 보완할 구간만 표시, 전체
  동작 나열 없음).
- 회귀 테스트(verify-cross-check) 4건 추가: 비동기 처리·이미지 사전 로드·캡처 표시 함수·
  카드-캡처 연결 확인.
- 서비스워커 캐시 버전: www v9→v10.

태권도 품새 수련 v3.60 전체 점검: 동작 데이터 정합성 투명화 + 다크모드 상태바 동기화 + UI 접근성 개선

2026-07-14 v3.60 전체 프로그램 점검(버그 수정 + UI/UX 개선)
- [핵심] 동작 데이터 정합성 투명화: 태극1장을 제외한 16개 품새(태극2~8장, 유단자 9종) 전부
  이미지 전사 자료의 확인된 동작명 수가 국기원 공식 동작 수보다 1~5개 적었다(예: 태극7장
  20/25, 고려 28/30). 기존에는 이 부족분이 화면·경계검출 양쪽에서 조용히 무시되어, 뒷부분
  동작 구간이 실제 영상과 어긋날 수 있었다. 수정: (1) buildGenericMovements가 부족한 번호를
  "동작 N(명칭 확인 필요)" 임시 항목으로 공식 수까지 채워 영상 구간 검출 수를 공식 동작 수와
  맞춤(앞쪽 확인된 동작들의 경계 정렬도 함께 개선), (2) 품새 선택 화면에 "OO개 중 XX개 확인됨"
  안내 문구 신설, (3) 구간 카드에 "⚠ 명칭 확인 필요" 배지 추가, (4) JSON 내보내기에
  confirmed_movement_names 필드 추가. 부족한 동작의 정확한 명칭 자체는 지어내지 않고 지도자
  확인이 필요하다고 정직하게 표시하는 방식을 택함(태권도 기술명은 도장 전문성 영역).
- 다크모드 상태바(theme-color) 동기화 버그 수정: 앱 내에서 수동으로 라이트/다크를 전환해도
  meta theme-color가 OS의 prefers-color-scheme만 따라가 상태바 색이 실제 화면과 어긋나던
  문제 수정. 이제 토글 시 두 meta 태그 모두 실제 적용된 테마 색으로 갱신됨.
- manifest.webmanifest(루트·www 모두) background_color/theme_color가 옛 팔레트(#0e1116)로
  남아있던 것을 현재 도장(道場) 다크 테마(#15120d)에 맞춤. PWA 설치 시 스플래시 화면 색상
  불일치 해결.
- 위저드 4단계("영상을 넣고 시작해요") 진입 검증 로직이 '다음' 버튼이 4단계에서 숨겨져 있어
  실제로는 한 번도 호출되지 않던 죽은 코드였음(분석 시작 버튼 비활성화로 기능상 문제는 없었으나
  안내가 보이지 않았음). renderWizard에 연결해 영상 미입력 시 안내 문구가 보이도록 수정.
- UI 접근성: 하단 제작자 정보 링크(센터장/인스타그램/블로그/전화번호)의 실제 탭 가능 영역을
  약 12px→33px로 확대(패딩+음수 마진으로 시각적 줄 간격은 그대로 유지).
- 정적 분석(ESLint) 및 Playwright 기반 전체 UI 동선(4단계 위저드, 품새 17종 전환, 테마 토글)
  점검 완료, 콘솔 오류 0건 확인. 기존 자동 테스트(스크립트 9종, 총 377개 검증) 전부 통과.
- 서비스워커 캐시 버전: www v8→v9, 루트 v6→v7.

2026-06-15 v3.55 영상-분석 통일성 회복 + 수동 조정 UI 가시성
- 문제1: 'X~Y번 연결동작' 자동 묶음이 두 카드를 하나로 합쳐(splice) 번호가 사라짐
  (예: 5·6 묶음→7번 사라지고 08로 건너뜀, 11·12 묶음→13번 사라짐). 영상 18동작과 어긋남.
  → applySemanticSceneRules에서 합치기(splice) 제거. 각 동작은 별도 카드로 모두 유지하고,
    연결 관계는 카드 '꼬리표(↔ 5~6번 연결동작 · 앞 동작과 한 자리에서 이어집니다)'로만 표시.
    검증: 20카드(준비+18+기합) 그대로, 7·13번 살아있음, 5·11번에 꼬리표.
- 문제2: '구간 수동 조정' 버튼이 잘 안 보임 → 조정 바를 주황 박스+굵은 버튼으로,
  renderSegments 끝에서 항상 패널 표시 호출. 분석 후 확실히 노출.

태권도 품새 수련 v3.54 Firebase 연동 — 보정 기록 자동 수집(corrections)
- 보정 발생 시 Firestore 'corrections'에 자동 전송(파일명 제외, 익명). 실패해도 로컬 저장.
- 보안규칙: corrections는 create만 허용, 기존 CMS 임시규칙 유지.

태권도 품새 수련 v3.53 수동 조정 '자동 결과로 되돌리기' 추가

태권도 품새 수련 v3.52 학습 1단계 — 사용자 보정 기록 수집

2026-06-14 v3.52 자동 분석 정확도 향상을 위한 '학습' 1단계: 보정 데이터 수집
- 목표: 사용자가 자동을 수동으로 고칠 때 '자동값 vs 사용자값'을 기록해, 2단계(규칙 보정)와
  이후 AI 학습의 재료를 축적한다. (진짜 AI 모델 학습은 데이터가 충분히 쌓인 뒤의 다음 단계)
- 구현:
  · 합치기 → 제거된 경계 시각·구간 기록(두 동작을 하나로 봐야 함).
  · 나누기 → 분할 위치·비율 기록(한 구간을 둘로).
  · 경계 이동 → 자동시각·사용자시각·차이(delta) 기록 = 자동 오차 그 자체.
  · 기기에 누적(localStorage, 최근 1000건) + 리포트(correction_log)에 포함 + JSON 내보내기.
    → 서버 없이 데이터 축적. 여러 기기/사용자 데이터를 모아 분석·학습에 사용 가능.
- 화면: '구간 수동 조정' 바에 '학습용 보정 기록 N건' 표시 + '보정 기록 내보내기' 버튼.
- 다음(2단계 예고): 모인 기록에서 패턴 추출 → 자동 경계 보정(예: adjust delta 평균만큼
  onset 이동, 특정 품새 구간 기본 묶음). 통계적 규칙이라 이 앱 안에서 구현 가능.

태권도 품새 수련 v3.51 자동 우선 + 균등경계 정지점 스냅 — 밀림 자동 감소

2026-06-14 v3.51 '자동 분석 우선, 부분 수동 보정' 전략 확립 + 자동 정확도 보강
- 전략: 자동 분석이 우선(수동 조정은 기본 꺼짐, 어긋난 부분만 토글로 보정).
- 자동 정확도 개선: refineSegmentBoundaries에서 정지점(valley)이 얕아 균등분할로 메우던
  경계를, 근처(±균등간격 45%)에 정지점이 있으면 거기로 '스냅'시킨다. 균등 위치 고정으로
  생기던 누적 밀림을 줄임. valley가 충분한 영상은 스냅 없이 그대로 정확(s1 검증: 19경계 전부
  valley, 균등 0 — 자동만으로 0.5~2.3초 자연스러운 정렬).
- 3단계 안전망: (1) 정지 뚜렷 → 자동 정확, (2) 정지 얕음 → 근처 정지점 스냅, (3) 그래도
  어긋남 → 사용자 수동 합치기/나누기(v3.50).

태권도 품새 수련 v3.50 구간 수동 조정 — 합치기/나누기로 밀림 직접 보정

2026-06-14 v3.50 자동 분석 밀림을 사용자가 직접 보정하는 수동 조정 기능
- 배경: 자동 분석은 초반 한 동작이 조금 밀리면 오차가 누적돼 3~4번 이후 점점 밀림.
  자동만으로는 한계가 있어, 사용자가 경계를 직접 보정하면 신뢰성이 근본적으로 올라감.
- 추가 기능(구간 화면 '구간 수동 조정' 토글):
  · 위 구간과 합치기: 잘못 나뉜 두 구간을 하나로(밀려서 한 동작이 두 칸일 때).
  · 둘로 나누기: 한 칸에 두 동작이 들어갔을 때 분할.
  · 조정 즉시 반영: 영상 재추출 없이 분석 때 보존한 모션/프레임 캐시로 구간만 재계산(빠름).
- 구현: 분석 시 boundaryDetection(경계·모션·프레임)과 movements를 캐시에 보존.
  mergeSegments/splitSegment/adjustBoundary가 경계 배열을 수정하고 rebuildSegmentsFromBoundaries로
  구간을 다시 만들어 화면 갱신. 스냅샷만 영상에서 다시 가져옴.
- 한계: 합쳤을 때 동작 묶음은 비례 추정이라 대체로 맞으나 완벽하진 않음. 실제 사용하며 다듬을 것.

태권도 품새 수련 v3.49 품새 끝점 정의 — 18번(기합 정지)까지만 분석, 마무리 복귀 제외

2026-06-14 v3.49 사용자 정의 품새 진행 구조 반영
- 구조: 차렷 →(태극1장 준비)→ 준비동작(예비) →(시작)→ 1~18번
  (18번은 지르기+기합 동시, 그 정지가 품새의 끝) →(바로)→ 준비복귀 →(쉬어)→ 차렷.
- 결정: 품새 본체(준비 + 1~18번)만 분석. 18번 기합 정지 이후의 '바로→준비복귀' 마무리는
  분석에서 제외(잘라냄). 마무리가 18번 구간에 섞여 늘어지던 문제 해결.
- 구현: detectActiveMotionRange에 '정점 기반 끝점(개선 B)' 추가 — onset의 대칭.
  마지막 큰 동작 정점(18번 기합) 직후 모션이 정점의 30% 이하로 가라앉는 정지 지점을
  분석 끝점으로. 끝부분 작은 복귀 움직임(마무리)은 큰 정점이 아니라 자동 제외됨.
- 검증(s1 영상): 활성 6.7~32.4초. 18번 29.8~30.5초, 기합 30.5~32.4초로 정상.
  35.5초 마무리 복귀 동작 제외 확인. 이전엔 18번이 30.5~33.7초로 마무리까지 늘어졌음.

태권도 품새 수련 v3.48 실제 태극1장 샘플 재검증 · 디코딩/신뢰도 수정

2026-06-15 v3.48
- 사용자 제공 태극 1장 영상 2개를 Chrome에서 실제 파일 선택→분석→JSON 저장 흐름으로 재검증
- HEVC(H.265) 샘플이 첫 프레임을 디코딩하지 못했는데도 0% 감지 점수를 만들던 문제 수정
- 실제 프레임·해상도 확인 전 분석 버튼 비활성화, 미지원 영상은 H.264 변환 안내 후 분석 차단
- 포즈 근거가 없을 때 연결동작 신뢰도가 '보통'으로 표시되던 문제 수정
- 연결동작의 두 속도 정점이 분리되지 않으면 신뢰도를 최대 '보통'으로 제한
- 복합동작 내부 단계 시각을 '추정'으로 명시하고 영상 근거 여부·추정 방식을 기록
- 태극 1장 공식 동작 수 18개와 준비·기합 포함 분석 단위 20개를 분리 표기
- 마지막 기합 장면이 20번으로 표시되던 오류를 '기합'으로 수정
- 실제 영상 재현 테스트 러너와 연결 신뢰도 회귀 테스트 보강

태권도 품새 수련 v3.47 태극1장 연결동작·복합동작 장면 설명

2026-06-15 v3.47
- 태극 1장 5~6번과 11~12번을 의미상 연결 장면으로 통합해 설명
- 중간 정지 시간, 속도 정점, 하체 안정, 관절 감지 품질로 연결 신뢰도 계산
- 태극 1장 14번과 16번 복합동작에 앱 분석용 내부 단계 표시
- 연결 장면은 첫 번째 동작 기준 점수 대신 연결 흐름과 최종 마무리 중심으로 평가
- 연결 장면의 개별 감점은 자동 확정하지 않고 지도자 확인으로 보류
- 완료 대표 장면 선택과 기존 교차검증 스크립트의 불일치 수정
- 연결·복합동작 합성 회귀 테스트 추가

태권도 품새 수련 v3.46 태극1장 동작 데이터 공식화 — 16개→18개 (빠진 17·18번 복구)

2026-06-14 v3.46 태극1장 movements를 국기원 공식 도해(18동작)에 맞춤
- 발견: 태극1장 분석 동작이 준비1+번호16+기합1=18개였음. 공식은 번호동작 18개인데
  17·18번이 빠지고, 18번이 "...및 기합" 이름 탓에 통째로 기합으로 분류돼 사라졌음.
  → onset이 맞아도 영상 18동작과 구조적으로 불일치(동작명·번호 어긋남의 핵심 원인).
- 수정: movements를 공식 도해 기준 18동작으로 재작성(14·16=앞차고지르기 복합).
  18번 동작과 기합을 분리(18번 지르기 + 별도 기합 음성 마커) → 18번이 동작으로 살아남.
- 결과: 준비1 + 번호동작 18 + 기합1 = 20개로 공식 일치. s1 영상 검증 시 준비 0~6.7초,
  1~18번 순서 정확, 기합 말미 정렬 확인.
- 참고: 좌우·서기 세부는 도해 판독 기반이라 지도자 검수 권장. 태극2~8 및 유단자 품새도
  동일하게 techniques 배열에 준비/기합/마무리가 섞여 있어 같은 정비 필요(향후).

태권도 품새 수련 v3.45 정점 기반 onset — 여러 샘플 검증으로 준비 종료점 안정화

2026-06-14 v3.45 준비 종료점(onset)을 '정점 기반'으로 재정의 + 다중 샘플 검증
- 배경: onset을 '임계 초'로 잡으니 영상마다(준비 길이·fps) 빗나가 준비가 1~3칸을 먹거나
  1번이 늦게 시작. 임계 튜닝으로는 수렴 불가.
- 해결: onset을 '초'가 아니라 '첫 큰 동작 정점' 기준으로 재정의. 영상에서 큰 동작 정점
  (최대의 45%↑) 중 첫 번째를 찾고, 그 직전 최저점(준비 완성 정지)을 onset으로 잡는다.
  동작 정점이 기준이라 영상 길이·fps·준비 길이가 달라도 자동으로 맞는다.
- 검증: 태극1장 샘플 4종(정면 39·126초, 후면 61·44초)으로 동시 검증.
  · 짧은 시연영상(실사용 시나리오): 준비 0~6.7초, 1번 6.7초 시작 — 영상 7.3초 1번 동작과 일치.
  · 후면 따라하기 영상: 준비 거의 없음(0초)으로 자동 인식.
- 한계: 중간에 설명·정지가 섞인 긴 교육영상은 그 정지를 한 구간으로 잡을 수 있음(원래 분석
  대상 아님, 참고용). 실사용 짧은 시연영상에서는 정확.

태권도 품새 수련 v3.44 준비자세 1칸 고정 — 동작 밀림(준비가 3칸 차지) 해결

2026-06-14 v3.44 긴 준비자세가 동작 여러 개로 쪼개지던 문제 수정
- 증상(반복): 준비자세가 길면(영상 앞 ~7초 정지) 준비~3번이 전부 '시작 전 정지'에 배정되고
  진짜 1번 동작이 4~5번째로 밀림. 프레임 확인으로 0~7초가 전부 준비자세임을 확정.
- 원인: v3.42에서 '준비 있으면 0초부터 N등분' → 긴 준비 정지 안에 가짜 경계가 여러 개 생김.
- 수정: 준비 종료점(onset=활성구간 시작)을 찾아 0~onset 전체를 '준비' 1칸으로 고정하고,
  1번~마지막은 onset 이후만 (동작 수-1)로 N등분. 활성구간 onset 감지도 '정점 대비 30%
  + 1.5초 내 큰 동작 지속' 조건으로 보강해 준비 흔들림에 덜 속도록 함.
- 한계(정직): onset의 초 단위 미세 위치는 12fps 시뮬레이션과 실제 앱(fps 다름)이 다를 수
  있음. 구조(준비 1칸 + 1번부터 정렬)는 옳으며, onset 0.5~1초 미세조정은 실제 앱 확인 후 진행.

태권도 품새 수련 v3.43 스냅샷 시점 개선 — 각 구간 대표 프레임을 '동작 맺음'으로

2026-06-14 v3.43 구간 정렬은 정확해졌고, 대표 프레임을 동작 완성 순간으로 보정
- 배경: v3.42에서 준비~동작 번호 정렬은 실제 앱에서 정확해짐(준비 0~2.2초, 1·2·3·4번
  순서 정상). 다만 각 구간 카드의 스냅샷이 동작 맺음이 아니라 전환 중간/다음 준비자세를
  잡아, 막기 카드에 준비 자세가 보이는 등 영상-표시 불일치가 남음.
- 원인: chooseCompletionSnapshotTimes가 구간 후반의 '단순 최저 모션'을 잡아, 다음 동작
  준비자세(낮은 모션)를 대표 프레임으로 선택할 수 있었음.
- 수정: 구간 중반~후반(40~88%)에서 모션 정점(동작 수행) 이후 속도가 정점의 35% 이하로
  처음 떨어지는 '맺음' 지점을 스냅샷으로 선택. 동작이 완성돼 멈추는 순간을 잡는다.
- 검증(태극1장 표본): 1번 스냅 9.0초가 왼쪽 아래막기 완성 자세와 일치. 스냅이 구간의
  75~87% 지점(맺음 직후)에 안정적으로 위치.

태권도 품새 수련 v3.42 재설계 — 영상-분석 통일성 (v3.39~v3.41 충돌 로직 전면 제거)

2026-06-14 v3.42 v3.38(원본)로 되돌린 뒤 단일 원칙으로 재설계
- 배경: v3.39~v3.41에서 공백 가드·펄스 분할·준비 분리를 잇따라 넣으며 로직이 충돌,
  실제 앱에서 동작이 14초대에 압축되거나 한 칸씩 밀리는 문제가 반복됨(시뮬레이션만 통과).
- 조치: 깨끗한 v3.38 원본으로 리셋 후, '영상-분석 통일성' 하나의 원칙으로 단순 재구성.
- 핵심 원칙(detectMotionBoundaries):
  1) 동작 수(count)만큼 '항상' N개 구간을 만든다(refineSegmentBoundaries). 동작을 묶지 않음.
     정지점이 뚜렷하면 그 위치, 부족하면 그만큼만 균등분할로 메움 → 절대 실패하지 않음.
  2) 구간 k → 동작 k 1:1 직접 매핑(정점 비례 재배분 제거).
  3) 준비자세(movements[0].isPrep)가 있으면 분할 시작을 영상 0초로 → '준비' 카드가
     영상 앞 준비자세 시간을 차지하고 1번부터 밀리지 않음.
  4) 표시 번호는 구간 순번이 아니라 실제 동작 번호(moveNo): 준비→"준비", 기합→"기합".
- 제거: 공백 가드(엉뚱한 덩어리 선택 주범), 펄스 fold, directOneToOne, standard 스키마 의존
  → 영상 특성(정지·끊김·fps)에 덜 민감.
- 검증(태극1장 표본): 준비 0~7.8초, 1~18번 순서 정확, 11.3초 영상동작과 3번 카드 일치.
  단, 12fps 시뮬레이션 기준이므로 실제 앱에서 재확인 필요.

태권도 품새 수련 v3.37 강유(剛柔) 분석 모드 추가

2026-06-13 v3.37 강유 분석 모드 + 완급·리듬 체크리스트 (수련/경기 양쪽 적용)
- 훈련 목적 아래 "강유(剛柔) 분석 추가" 토글을 켜면, 수련품새·경기품새 어느 모드에서든 강유 분석이 함께 표시됩니다.
- 동작별 강유 판별: 속도 곡선에서 가속 대비(정점÷준비 평균)와 정점 도달 프레임 수를 계산해 강(剛)·유(柔)·전환으로 분류하고, 구간 카드에 표시합니다.
- 강유 종합 카드: 강/유/전환 분포와 함께 완급조절 리듬 체크리스트 5가지(정점 간격 일정함, 가속 대비, 맺음 정지, 연속/끊는 동작 구분, 강약 물결)를 영상 데이터로 채워 보여줍니다.
- 토글은 분석 후에도 즉시 켜고 끌 수 있어, 같은 결과를 강유 관점으로 다시 볼 수 있습니다.
- 강유 판별 회귀 테스트(test:gangyu)를 추가했습니다.
- 별도 제공: ‘강유 동작 최적화 체크시트’ 1페이지 인쇄용 문서(데이터 추출 가이드·판별 질문·리듬 체크리스트·시작 5단계).
- 강유(剛柔) 분석 추가 토글을 경기품새 설명과 동일하게 가로 전체 폭으로 펴 우측 여백을 없앴습니다(UI 정렬).

태극 품새 수련 v3.36 7개 샘플 교차검증 · HEVC 코덱 감지/경고

2026-06-13 v3.36 영상 신뢰성 교차검증 결과 반영, HEVC 미지원 경고 추가
- 업로드된 7개 샘플(27~73초)을 실제 MediaPipe로 분석해 교차검증했습니다. 7개 모두 관절 감지율 98~100%, 활성구간·자연구간·동작배분·단조성 정상으로 알고리즘 신뢰성이 확인됐습니다.
- '체감상 변화 없음'의 유력 원인을 발견했습니다: 7개 샘플 전부 HEVC(H.265) 코덱입니다. 일부 모바일 브라우저·WebView는 HEVC를 디코딩하지 못해, 영상이 보여도 분석이 진행되지 않거나 빈 결과가 나옵니다. 그러면 알고리즘 개선이 체감될 수 없습니다.
- 영상 로드 시 파일의 앞/뒤를 읽어 HEVC를 감지하고, 현재 브라우저가 HEVC를 디코딩 못 하면 "H.264로 변환 후 재시도"를 명확히 안내합니다(moov가 파일 끝에 있는 경우까지 검사).
- 영상 디코딩 실패(error 이벤트)도 직접 잡아 코덱 문제일 수 있다고 안내합니다.
- HEVC 감지 회귀 테스트(test:hevc)를 추가했습니다.

태극 품새 수련 v3.35 리포트 모드별 정비 (묶음 구간 반영)

2026-06-13 v3.35 구간 묶음에 맞춰 리포트 수정, 두 모드 성격 강화
- 수련품새: 각 구간에 "동작이 잘 보이나요?"(동작 확실성)를 또렷함/대체로 또렷/이어진 동작/또렷하지 않음으로 쉽게 표시하고, 문제·수정을 초등학생도 이해할 쉬운 말로 제공합니다. 요약에 또렷하게 된 동작 수와 이어진 동작 수를 표시합니다.
- 경기품새: 각 구간에 가감점(약 -0.1/-0.3 가능성 또는 판정 보류)을 명확히 표시하고, 점수를 가장 많이 되돌릴 핵심 수정 포인트 한 가지를 짚습니다. 요약에 참고 감점 합계(추정)를 더했습니다.
- 묶인 구간(여러 동작이 한 구간)에는, 수련품새는 "두 동작이 이어졌다"고 쉽게 알리고, 경기품새는 "감점은 동작별로 지도자 확인 권장"이라고 안내합니다.
- JPG/텍스트 리포트 출력도 동작 확실성·가감점·핵심 수정·묶음 표시를 반영하도록 수정했습니다.

태극 품새 수련 v3.34 하이브리드 구간 탐지 (전 품새 적용)

2026-06-13 v3.34 개선 A·B + 동작 묶음을 결합한 하이브리드, 모든 품새 적용
- 모든 품새(태극 1~8장, 고려·금강·태백·평원·십진·지태·천권·한수·일여)에 동일하게 적용됩니다. 품새별 동작 수(18~30)에 자동으로 맞춥니다.
- 개선 A(준비자세 제외): 영상 앞부분의 긴 준비 정지를 첫 구간에서 빼, 1번 동작이 비대해져 전체가 밀리는 문제를 줄입니다.
- 개선 B(긴 구간 분할): 평균보다 훨씬 긴 구간은 내부에 실제 멈춤(국소 최저점)이 있을 때만 추가로 분할합니다. 멈춤이 없으면 억지로 쪼개지 않아 가짜 경계가 생기지 않습니다.
- 동작 묶음: B로도 못 나누는 진짜 연속 동작은 한 구간에 묶어 동작명을 함께 표시합니다(예: "6~7번 묶음").
- 셋을 결합해 각 방식의 단점을 서로 메웁니다. A는 시작 밀림을, B는 묶음 과다를, 묶음은 B의 가짜 분할을 막습니다.
- 화면 설명을 "N개 구간"에서 "N개 동작을 멈춤 기준으로 나누고 이어지는 동작은 묶어 표시"로 정확하게 바꿨습니다.
- 하이브리드 B 분할·가짜 분할 금지에 대한 회귀 테스트를 추가했습니다.

태극 품새 수련 v3.33 동작 묶음 표시 · 경계 정확도

2026-06-13 v3.33 여러 동작이 한 구간에 들어갈 때 동작명을 묶어서 표시
- 경계를 동작 수만큼 억지로 만들지 않고, 영상에서 멈춤이 뚜렷한 만큼만 자연스럽게 끊습니다.
- 한 구간에 여러 동작이 들어가면(연속 동작이라 사이에 멈춤이 없을 때), 그 구간의 동작명을 "A + B"로 묶어서 보여주고 몇 번~몇 번 동작인지 표시합니다. 예: 한 구간에 6·7번이 함께 들어가면 "왼 앞서기 몸통안막기 + 오른 앞서기 몸통안막기 [6~7번 묶음]".
- 동작을 각 구간 길이에 비례해 배분합니다. 긴 구간일수록 더 많은 동작이 묶입니다.
- 여러 동작이 묶인 구간이 있으면 리포트 상단에 안내하고, 각 동작 끝에서 잠깐 멈춰 촬영하면 분리된다고 알려줍니다.
- 실제 태극 2장 영상으로 검증해, 기존에 동작명이 한 칸씩 밀리며 꼬이던 문제 대신 묶인 동작을 정직하게 표시하도록 했습니다.
- 경계 탐지·동작 배분 회귀 테스트를 실제 함수 기준으로 갱신했습니다.

태권도 품새 수련 v3.32 동작 경계 탐지 정확도 개선

2026-06-13 v3.32 valley prominence 기반 경계 탐지 · 동작명 밀림 경고
- 동작을 끊는 방식을 "시간 균등분할 + 근처 정지점"에서 "실제 정지점(valley)의 깊이(prominence) 기반 선택"으로 바꿨습니다. 동작 길이가 들쭉날쭉해도 실제 멈추는 지점에서 끊습니다.
- 동작 길이를 균등으로 강제하던 ±38% 제약을 제거했습니다. 짧은 동작과 긴 동작이 섞여도 경계가 한쪽으로 밀리지 않습니다.
- valley 깊이 임계를 영상마다 자동 적응시키고, 구간 감지 민감도 슬라이더로 미세 조정할 수 있게 유지했습니다.
- 뚜렷한 멈춤이 부족하면 부족한 경계만 시간 균등으로 안전하게 메우고(절대 실패하지 않음), 그 비율이 높으면 "동작명이 한 칸씩 어긋날 수 있다"고 리포트에 안내합니다.
- 각 경계의 신뢰도(valley/균등추정)를 결과 데이터에 기록합니다.
- 합성 데이터 200회 평균에서 불균등 동작의 경계 평균오차가 기존 대비 약 절반으로 줄었습니다(0.33s→0.15~0.18s). 경계 탐지 회귀 테스트(test:boundary)를 추가했습니다.

태권도 품새 수련 v3.31 분석 오류 수정 · 안정화

2026-06-13 v3.31 분석 중 타임스탬프 오류 수정 · 불안정 스캔 제거
- 일부 영상에서 분석이 중단되던 원인(MediaPipe detectForVideo 타임스탬프 비단조)을 수정했습니다. 모든 포즈 검출을 단조 증가 타임스탬프로 감싸는 detectPoseAt 래퍼를 도입해, seek 오차나 재분석으로 같은/역행 타임스탬프가 들어와도 오류 없이 진행합니다.
- 한 프레임 검출이 실패해도 분석 전체가 멈추지 않도록 빈 결과로 대체하는 내성을 추가했습니다.
- v3.30에서 시험 도입한 순방향 재생 기반 스캔이 실시간 재생 속도로 동작해 오히려 느리고 일부 환경에서 오류를 유발하던 문제를 확인하고, 검증된 빠른 seek 경로(waitForSeek fast)로 되돌렸습니다. 결과 프레임의 시각·좌표·개수는 기존 안정 버전과 동일합니다.
- 타임스탬프 단조 증가·오류 내성에 대한 자동 회귀 테스트(test:timestamp)를 추가했습니다.

태권도 품새 수련 v3.30 도장 리디자인 · 속도 · 오프라인 명조

2026-06-13 v3.30 정확성 보존형 속도 개선 · 도장 무드 리디자인 · 쉬운/수정중심 피드백 · 로컬 명조
- 전체 동작 전환점 스캔을 무작위 seek에서 순방향 재생 디코딩으로 변경해 같은 프레임을 더 빠르게 수집합니다. 채택 허용오차를 기존 seek 허용오차와 동일하게 두고, 못 맞춘 목표만 기존 정밀 seek로 자동 폴백하므로 결과 프레임의 시각·좌표·개수는 기존과 동일합니다.
- 전통 도장(道場) 무드 리디자인: 명조 제목 서체, 단청 청·홍 포인트, 한지 질감 카드, 品勢 워터마크, 단청 띠. 다크/라이트 테마 토글을 추가하고 선택을 저장합니다(기본 다크).
- 수련품새 리포트를 초등학생도 이해할 수 있는 쉬운 말(무엇이 아쉬운지 / 이렇게 고쳐요)로 바꿨습니다.
- 경기품새 리포트를 반복 안내 대신 수정 우선순위(1·2·3순위로 어디를 어떻게 고치면 점수가 오르는지)로 바꿨습니다.
- 명조 서체(Noto Serif KR)를 글자 서브셋하여 로컬(www/assets/fonts)에 포함하고 Google Fonts 의존성을 제거했습니다. 서비스워커 캐시에 등록해 오프라인에서도 제목 서체가 정상 표시됩니다.
- 교차검증 스크립트와 오프라인 자산 점검에 위 변경에 대한 검증 항목을 추가했습니다.

태권도 품새 수련 v3.29 속도·신뢰성 교차검증

2026-06-08 v3.29 정확성 보존형 분석 속도 개선
- 파트너 로고 아래의 용인대 국가대표태권도 인스타그램 문구와 텍스트 링크를 제거하고, 로고 클릭 연결만 유지했습니다.
- 하단 `김동규 센터장` 이름에 네이버 블로그 소개 페이지 링크를 연결했습니다.
- 파트너 로고를 몸가짐운동센터 로고와 제작 정보 위로 이동하고, 용인대 국가대표태권도 로고 및 `@yongin_kr`을 누르면 인스타그램이 새 창으로 열리도록 연결했습니다.
- v3.28의 안정적인 1패스 경계 스캔과 실제 mediaTime 동기화를 유지했습니다.
- 경계 탐색에서 이미 검출한 포즈 프레임을 구간 평가에 안전하게 재사용해 중복 seek·포즈 검출을 줄였습니다.
- 재사용 프레임은 목표 시각 0.18초 이내, 고유 프레임, 관절 감지 완료 조건을 모두 만족해야 합니다.
- 조건을 만족하지 못한 구간은 기존 정밀 분석 경로로 자동 폴백합니다.
- 일반적인 45~90초 합성 조건에서 포즈 검출 호출이 평균 약 31.3% 감소할 것으로 검증했습니다.
- 분석 리포트 JSON에 재사용·정밀 폴백·절감 검출 수와 실제 분석 시간을 기록합니다.

2026-06-08 v3.28 분석 안정화 · 하단 로고 가로 전체
- v3.27에서 도입한 2단계 스캔(거친 스캔으로 활성 구간 추정)이 일부 영상에서 활성 구간을 잘못 좁게 잡아 구간·리포트가 생성되지 않던 문제를 확인하고, 검증된 1패스 정밀 스캔(초당 약 3.5회)으로 되돌렸습니다. 다양한 영상 길이·동작 수에서 구간·리포트 생성을 재검증했습니다.
- 하단 파트너 로고를 가로 전체 폭으로 펼치고(좌우 끝까지) 높이만 작게 유지했습니다.
- 구간 영상 팝업 재생 수정(전용 blob 주소)과 분석 mediaTime 동기화는 그대로 유지했습니다.

태권도 품새 수련 v3.27

2026-06-08 v3.27 하단 로고 축소 · 2단계 분석 · 구간 재생 수정
- 하단 파트너 로고를 더 작게(가로 비율 유지, 좌우 여백 유지) 줄여 상단 로고보다 작게 배치했습니다.
- 동작 전환점 탐색을 2단계(거친 스캔으로 영상 앞뒤 정지 구간을 찾아 건너뛰고, 활성 구간만 정밀 스캔)로 바꿔 분석 시간을 줄였습니다. 거친 스캔 표본도 정밀 분석에 재사용해 정확도를 보존하며, 절감 효과가 없는 영상은 기존 1패스 정밀 스캔으로 자동 폴백합니다.
- 구간 분석에서 영상 클릭 시 팝업이 재생되지 않던 문제를 수정했습니다(메인 영상과 팝업 영상이 같은 blob 주소를 공유해 일부 기기에서 로드가 실패하던 문제 → 팝업 전용 영상 주소를 별도 생성).
- 2단계 스캔 적용 후에도 동작↔명칭 1:1 매핑 무결성을 320회 합성 시뮬레이션으로 재교차검증했습니다.

태권도 품새 수련 v3.26

2026-06-08 v3.26 하단 정보 영역 정리
- 하단 로고가 텍스트 높이에 맞춰 세로로 늘어나며 생기던 흰 여백을 제거했습니다(원본 비율 유지).
- 로고 컬럼 폭과 안쪽 여백을 줄여 더 컴팩트하게 배치했습니다.
- 우측 제작/연락처 글자 크기와 줄 간격을 줄여 줄 맞춤을 정리했습니다(모바일 11px).

태권도 품새 수련 v3.25

2026-06-08 v3.25 측정/분석 속도 최적화 및 교차검증
- 동작 전환점(구간 경계) 탐색을 초당 3.5회로 조정해 포즈 검출 횟수를 약 16~30% 줄였습니다.
- 경계 탐색은 좌표만 필요하므로 화면 페인트 대기를 생략하는 빠른 seek 경로를 사용합니다.
- 세그먼트 샘플과 대표 스냅샷은 기존 정밀 경로(실제 mediaTime)를 유지해 영상-분석 싱크와 동작-명칭 매칭 정확도를 그대로 보존합니다.
- 동작당 누적되던 분석 루프 대기(10ms)를 제거했습니다.
- 속도 최적화 후에도 동작↔명칭 1:1 매핑 무결성을 400회 합성 시뮬레이션으로 재교차검증했습니다(경계=동작수+1, 단조증가, 스냅샷 구간 내부 100% 통과).

태권도 품새 수련 v3.24 교차검증 통합본

2026-06-08 v3.24 전체 통합 교차검증
- 사용자 제공 v3.23 전체 내용을 기준으로 유지했습니다.
- 녹화 영상과 분석 프레임의 실제 mediaTime 동기화 보완을 유지했습니다.
- 붉은 `태권도 품새` 붓글씨 앱 아이콘을 192·512·1024 크기로 통합했습니다.
- 루트 배포와 www 직접 배포 모두에서 아이콘이 실제 프로그램을 열도록 연결했습니다.
- 동작-구간 경계 및 대표 스냅샷 매핑을 실제 자동 테스트 384회로 교차검증합니다.
- 인코딩된 업로드 안내 파일명을 정상 한글 파일명으로 복구했습니다.

2026-06-08 녹화영상-분석 싱크 보완 및 교차검증
- 분석 프레임 시각을 requestVideoFrameCallback의 실제 mediaTime으로 기록해 영상과 분석 결과를 정렬했습니다.
- seek 후 화면 페인트 완료를 기다린 뒤 포즈를 검출하도록 보완했습니다(미지원 기기는 double-rAF 폴백).
- 구간 경계 탐색과 완료 스냅샷 캡처도 동일한 실제 프레임 시각 기준으로 통일했습니다.
- 동작↔명칭 1:1 매핑 무결성을 384회 합성 시뮬레이션으로 교차검증했습니다(경계=동작수+1, 단조증가, 스냅샷 구간 내부 100% 통과).
- 푸터의 문의 정보를 블로그 아래 한 줄로 분리했습니다.

태권도 품새 수련 v3.22

2026-06-08 보완 및 교차검증
- 홈 화면 아이콘을 누르면 실제 프로그램 화면이 열리도록 루트 및 www 배포용 manifest와 서비스워커를 연결했습니다.
- KTA 2026 규칙 기준의 정확도·균형·표현력 항목을 리포트에 연결했습니다.
- 구간별 결과를 `확인 가능`, `추정`, `지도자 확인 필요`로 구분했습니다.
- 경기품새 감점은 -0.1/-0.3 `참고 감점 가능성`으로만 표시하며, 감지율이 낮으면 판정을 보류합니다.
- 리포트를 `구간별 상세 분석`, `핵심 모션 매칭`, `지도자 추천 피드백 및 훈련법` 구조로 보완했습니다.
- 시선, 품새선, 정확한 목표 높이, 손발 모양, 공식 감점은 영상 AI가 확정하지 않도록 제한했습니다.

v3.22 핵심 변경
- 수련품새 리포트의 `전체 부분`도 사진 상단 중앙, 간략한 문제점 하단 구조로 변경했습니다.
- 전체 부분의 반복되는 긴 피드백을 핵심 문제점 한 줄로 줄였습니다.
- 앱 하단에 몸가짐운동센터 로고와 배포·제작자·문의 정보를 추가했습니다.

v3.21 핵심 변경
- 홈의 `훈련 시작` 아래에 펼쳐보는 `프로그램 사용 설명서` 버튼을 추가했습니다.
- 프로그램 목적, 권장 촬영 방법, 5단계 사용 순서를 간단하게 정리했습니다.

v3.20 핵심 변경
- 앱 이름을 `태권도 품새 수련`으로 통일했습니다.
- 경기품새 리포트의 `전체 부분` 카드도 사진 상단 중앙, 피드백 하단 구조로 변경했습니다.

v3.19 핵심 변경
- 분석 시작 버튼을 화면 폭에 맞는 단일 주 버튼으로 정리했습니다.
- `훈련 분석 요약` 명칭을 `리포트`로 통일했습니다.
- 우선 확인 카드의 영상 캡처를 위쪽 중앙에 배치하고 피드백을 아래에 배치했습니다.
- 구간 감지를 초당 약 3회로 촘촘하게 수행하고 실제 움직임 최저점을 우선 선택합니다.

v3.18 핵심 변경
- 경기품새 피드백을 2026 경기규칙의 정확성·표현성 기준에 연결했습니다.
- 기술별로 서기, 막기, 지르기, 차기의 정확성 감점 위험 확인 항목을 표시합니다.
- 분석 전에 관절 움직임을 탐색해 구간 경계를 가까운 정지점으로 자동 보정합니다.
- 구간 감지 민감도를 보통, 높음(권장), 매우 높음으로 선택할 수 있습니다.
- 관절이 감지되지 않은 장면은 거짓 정지점 후보에서 제외합니다.

사용 흐름 및 UI 보완
- 선택한 품새와 영상 파일명에서 확인된 품새가 다르면 경고창을 표시합니다.
- 영상 내용 자체를 확정 판별하는 기능은 아니며 파일명에 품새명이 있을 때만 확인합니다.
- 재분석(초기화) 버튼은 화면에서 숨기고, 파일 또는 품새 변경 시 분석 결과를 자동으로 정리합니다.
- 구간 카드를 누르면 현재 위치를 유지한 채 팝업 영상으로 해당 구간을 재생합니다.
- 구간 카드의 `눌러 재생` 문구를 제거하고 기술명 색상을 밝게 보정했습니다.
- `결과지`를 `훈련 분석 요약`으로 변경했습니다.
- 훈련 분석 요약 상단에서 A4 한 장 크기의 JPG를 만들고 저장할 수 있습니다.

교차검증 보완 사항
- 정지 안정성은 마지막 구간의 잔여 움직임 속도와 속도 변화량을 함께 평가합니다.
- 일정한 속도로 계속 움직이는 장면이 정지 100점으로 판정되는 문제를 수정했습니다.
- 분석 표본 수를 일반적인 60~90초 영상에서 초당 약 1.5~2회 이하로 제한했습니다.
- 어깨와 골반 좌표의 신뢰도가 낮으면 신체 크기 정규화 계산에서 제외합니다.
- 현재 앱 문구와 자동 검증 문구를 일치시켰습니다.
- 정지 오판과 과도한 분석 표본 수의 재발을 막는 합성 엔진 테스트를 추가했습니다.

자동 검증 명령
- npm run assets:check
- npm run verify:cross
- npm run test:engine
- npm run test:all

주의 사항
- 이 앱은 영상 기반 훈련 보조 도구이며 공식 심사 또는 경기 판정을 대체하지 않습니다.
- 태극 1장은 상세 MVP 데이터입니다.
- 나머지 품새의 정확한 동작명과 지도 포인트는 지도자 검수 후 지속 보정이 필요합니다.

GitHub 업로드 방법
- 이 폴더 안의 모든 파일과 폴더를 GitHub 저장소 최상단에 그대로 올리세요.
