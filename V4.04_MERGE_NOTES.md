# 태권도 품새 수련 v4.04 통합 메모

## 통합 목표

- 영상은 기기에서 분석하고 원본 영상은 서버에 올리지 않습니다.
- 준비자세 인식과 첫 동작 움직임 급상승을 교차검증해 시작점을 잡습니다.
- 사용자가 시작점과 구간 경계를 고치면 같은 기기에서 반복 보정을 학습합니다.
- 익명 숫자 보정 기록은 관리자가 검토해 전체 사용자용 타이밍 보정으로 반영할 수 있습니다.
- Firebase Storage 업로드 기능을 제외해 무료 운영 구조를 유지합니다.

## v4.04 핵심 변경

- 첫 동작 시작점 검출을 실제 분석 흐름에 연결하고 이동량 안전 제한을 적용했습니다.
- 일정한 움직임을 시작 급상승으로 오인하지 않도록 폴백 검사를 추가했습니다.
- 최소 3회 이상, 보정 방향 일치율 67% 이상일 때만 기기별 학습값을 적용합니다.
- 기기별 학습 이동량은 ±0.6초, 전체 보정은 ±1.5초로 제한합니다.
- 연쇄 경계 보정 기록이 Firestore 규칙에서 거부되던 문제를 수정했습니다.
- 서비스워커 캐시를 v55로 갱신했습니다.

## 검증 범위

- 전체 자동 회귀 테스트 통과
- 시작점 급상승 검출 및 안전 폴백 합성 테스트 통과
- 기기별 반복 학습, 최소 표본, 방향 일치, 보정 상한 테스트 통과
- 배포 루트에서 `www/index.html` 자동 진입 확인
- 모델, MediaPipe WASM, 서비스워커의 HTTP 200 응답 확인
- 390×844 모바일 화면에서 가로 넘침 없음 확인

실제 촬영 환경의 모든 카메라·거리·조명·품새를 코드 테스트만으로 100% 보증할 수는 없습니다. 정식 출시 전에는 대표 실제 영상 묶음으로 시작점 오차와 구간 오차를 별도 측정하고, 수집된 보정값은 관리자 검토 후 전체 보정에 반영해야 합니다.
