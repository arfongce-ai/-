# 학습 리포트 기능 설정 안내 (배포 전 필수)

v3.93부터 "관리자 모드" 안에 **학습 리포트** 섹션이 추가되었습니다. 여러 영상에 걸쳐 쌓인
보정 기록(합치기·나누기·이동)을 자동으로 분석해서, 어느 품새의 어느 동작에서 자동 감지가
자주 틀리는지, 어느 방향으로 치우쳐 있는지를 보여주고 규칙 개선을 제안합니다.

**중요: 규칙 개선 제안 대부분(분류 로직·민감도 조정 등)은 여전히 자동 반영되지 않습니다** —
리포트는 제안만 보여주고, Claude에게 요청해야 코드가 바뀝니다. 다만 v3.95부터 "경계 타이밍"
치우침 제안만은 예외로, 관리자가 직접 승인 버튼을 누르면 코드 배포 없이 즉시 모든 사용자에게
반영됩니다(아래 5번 참고) — 이 정도가 지금 데이터 규모에서 안전하게 자동화할 수 있는 현실적인
선입니다. 그 외는 여전히 "반자동"입니다 — 분석은 자동, 적용은 승인 후 Claude에게 요청.

## 왜 로그인이 필요한가요

보정 기록(Firestore `corrections` 컬렉션)은 스팸 방지를 위해 익명 읽기가 원천 차단되어
있습니다. 이 리포트를 보려면 진짜 로그인(이메일+비밀번호)이 필요합니다 — 기존 배너 설정용
PIN(`0713`)과는 완전히 별개이며, PIN은 보안이 아니라 가벼운 안내용이라 이 용도로는 쓸 수
없습니다.

## 설정 절차 (Claude가 대신 할 수 없는 부분 — 직접 해주셔야 합니다)

### 1. Firebase 콘솔에서 이메일/비밀번호 로그인 켜기
1. https://console.firebase.google.com → `momgagym-cms` 프로젝트 선택
2. 왼쪽 메뉴 **Authentication** → **Sign-in method** 탭
3. **이메일/비밀번호** 제공업체를 사용 설정으로 켜기

### 2. 관리자 계정 만들기
1. 같은 **Authentication** 화면의 **Users** 탭 → **사용자 추가**
2. 실제 사용하실 이메일과 비밀번호를 입력해 계정 생성
   (예: `dgkim@momgagym.com` — 실제 쓰실 이메일로)

### 3. 이 저장소의 firestore.rules에 그 이메일 반영
`firebase/firestore.rules` 파일 맨 아래쪽, 다음 부분을 실제 이메일로 바꿔주세요:

```
function ADMIN_EMAILS() {
  return ['admin@example.com'];   // ← 여기를 2번에서 만든 실제 이메일로 교체
}
```

여러 명이 볼 수 있게 하려면 배열에 이메일을 추가하면 됩니다:
`return ['admin1@example.com', 'admin2@example.com'];`

### 4. 규칙 배포
Firebase CLI가 설치·로그인되어 있다면:
```
firebase deploy --only firestore:rules
```
CLI가 없다면 Firebase 콘솔 → Firestore Database → 규칙 탭에서 `firestore.rules` 내용을
그대로 붙여넣고 게시해도 됩니다.

## 사용 방법
1. 앱 하단 "관리자 모드" 버튼 클릭
2. PIN 입력(배너 설정용, 기존과 동일)
3. 아래로 스크롤 → "학습 리포트 · 보정 데이터 분석" 섹션에서 2번에서 만든 이메일/비밀번호로 로그인
4. 자동으로 현재까지 쌓인 보정 기록을 분석해 보여줍니다. 표본이 위치당 10건 이상 쌓이면
   "규칙 보정 가능", 200건 이상이면 "표본 충분(ML 후보)"로 표시됩니다.
5. **v3.98부터: 타이밍 보정은 이 화면을 아예 안 열어도 자동으로 반영됩니다.** 이 계정으로
   로그인된 채(한 번 로그인하면 이 기기엔 계속 유지됩니다) 평소처럼 앱을 쓰기만 하면, 12시간에
   한 번씩 백그라운드에서 조용히 확인해서 안전 기준(표본 10건 이상·방향 일치 70% 이상)을 넘는
   타이밍 제안을 자동으로 게시합니다. ±1.5초 상한이 걸려 있고, 이미 감지된 경계의 위치만
   미세조정할 뿐 동작 분류·명칭·개수는 절대 바꾸지 않습니다. 무엇이 언제 자동 반영됐는지는
   이 화면의 "최근 자동 반영 이력"에서 확인할 수 있고, "제거" 버튼으로 언제든 되돌릴 수 있습니다.
   급하게 지금 바로 반영하고 싶은 제안이 있으면 카드의 "지금 적용" 버튼도 그대로 남아 있습니다.
   그 외(민감도·최소 구간 길이 등 코드 로직 조정이 필요한 제안)는 자동 반영 대상이 아닙니다 —
   내용을 캡처하거나 그대로 Claude에게 전달해서 반영을 요청하세요.

## 참고
- 이 절차를 아직 안 하셔도 앱의 다른 기능(분석·수동 조정·영상별 보정 저장)은 전혀 영향 없습니다.
- 위 1~2번(Firebase 콘솔 설정)은 Claude가 대신 해드릴 수 없는 부분입니다 — Google 계정
  권한이 필요한 작업이라 직접 진행해주셔야 합니다. 3~4번(코드 수정·배포)은 이미 3번까지는
  플레이스홀더로 준비해뒀고, 실제 이메일로 바꾸는 것과 배포만 남았습니다.
