Place MediaPipe Tasks Vision offline files here.

Required:

```text
vision_bundle.mjs
wasm/vision_wasm_internal.wasm
wasm/vision_wasm_internal.js
```

Run this from the project root when online:

```powershell
npm run assets:download
```
